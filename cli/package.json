{
  "name": "samkoma-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "samkoma": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "node --experimental-strip-types src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@samkoma/core": "*",
    "@types/node": "^22.10.2",
    "samkoma-client": "*",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "~2.1.8"
  }
}
