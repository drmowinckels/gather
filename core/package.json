{
  "name": "@samkoma/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared domain logic for samkoma (dates, slot grid, ranking). Source-only; consumers bundle it.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "~2.1.8"
  }
}
