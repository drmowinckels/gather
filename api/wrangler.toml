name = "samkoma-api"
main = "src/index.ts"
compatibility_date = "2025-02-04"

# Production config (this file = the deployed state). For local `wrangler dev`,
# values are overridden by `.dev.vars`; tests override them in vitest.config.ts.
# ALLOWED_ORIGINS is a comma-separated allowlist for CORS.
[vars]
ALLOWED_ORIGINS = "https://samkoma.drmowinckels.io"
WEB_BASE_URL = "https://samkoma.drmowinckels.io"
CREATE_LIMIT = "30"      # max poll creations per IP per minute
MAX_RESPONSES = "1000"   # max distinct respondents per poll

[[d1_databases]]
binding = "DB"
# D1 name is fixed at creation (no rename); the live database is "gather".
# The binding uses database_id below, so the rebrand does not affect runtime.
database_name = "gather"
database_id = "cb6ccb98-b67d-4087-9398-4afc97b11a21"
migrations_dir = "migrations"

# Daily cleanup of expired polls (03:17 UTC).
[triggers]
crons = ["17 3 * * *"]
