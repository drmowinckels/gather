// Slot-grid helpers live in @samkoma/core (shared with the web grid). Re-exported
// here so the rest of the Worker imports them locally.
export { timeSlots, slotKey, validSlotKeys } from "@samkoma/core";
